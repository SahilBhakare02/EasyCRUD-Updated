apiVersion: v1
kind: Pod
metadata:
  name: backend
  labels:
    app: backend
spec:
  containers:
    - name: backend
      image: iamsahil21/easycrud2-jenkins:backend
      imagePullPolicy: Always
      ports:
        - containerPort: 8080
